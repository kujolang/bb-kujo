# Session Memory · bb-kujo · Published Kujo adapter · 2026-09-12

- Repository: https://github.com/kujolang/bb-kujo; local /Users/robertdevore/2026/Kujolang/kujo-repos/bb-kujo, main.
- Milestones: initial research/adapter 013a12f and 20baac7; revised signature eb62706; verified editor compatibility repair c832771; public catalog/screenshots 2e9beb5. This state supersedes revision 1's missing-remote and unresolved-local-QA blockers.
- User correction: the original edge-only signature looked generic. The required visual direction is recognizably kujolang.ai, not just dark tokens. The revision uses the actual Kujo workflow hero, locally optimized to 66,810 bytes, a shell mark, mechanical rails and 117ms background-only slice glitches every nine seconds.
- Architecture: native bb.themes ID plugin:bb-kujo:kujo; centralized palette generates bb/code/ANSI/diffs. Static artwork and mark are embedded CSS variables; CSS pseudo-elements carry decoration. One inert content-script edge node owns hidden-tab pause and cleanup. No runtime network, timers, observers, text distortion or bb fork.
- Verification: nine tests and TypeScript/native build pass. All text clears AA through the conservative #363636 decorative composite. 100 cleanup cycles, five native reloads, responsive screens, theme switching and reduced motion pass. Native signature snapshots verify a 3px slice phase; reduced motion retains still artwork. Short performance samples are not all-day certification.
- Editor repair: stock bb 0.43.0 Monaco disposes a model before its editor and throws after edit/undo/panel switch. Dispose editor first, then saved model. Real full workspace QA now passes with zero exceptions using that repair. Source patch and an explicit SHA-256-locked apply/check/restore CLI are shipped separately; the skin never applies it. Unknown editor builds are rejected. Stock upstream is still affected; no upstream acceptance is claimed.
- Distribution: public main is pushed. package metadata and schemaVersion 2 marketplace.json use the actual GitHub URL. Native bb accepted the public HTTPS catalog with one entry and no error. It is a self-hosted Kujo catalog, not a bb-community approval. README has exact Git/local/catalog install and development commands.
- Evidence pointers: docs/research.md, architecture.md, qa.md, signature-qa.json, workspace-repaired-qa.json, monaco-compatibility.md, compatibility/monaco-dispose-order.patch and screenshots. Stock failure records remain available for comparison.
- Human review: original SignalBox sig_a829b547-b210-43b1-b96b-8879a796deb6 remains relevant for upstream review; correction Capture cap_3145564d-4a6d-4402-be65-1482c5c6c10e adds the verified fix evidence to cap_8561c059-d0b6-4e41-bc97-72e43fec7a0f. Exact/concept retrieval passes. No duplicate Signal or downstream task was created.
- Boundaries: as of 0.2.0, native Kujo light and dark are supported (supersedes the original dark-only boundary). Native-device certification, third-party iframe palettes and long-duration profiling remain unclaimed. bb-community review and upstream patch adoption are external follow-ups, not completed approvals.
- Next recall cues: bb-kujo published SiteKit adapter; Kujo workflow background glitch; Monaco disposal-order compatibility repair; self-hosted Kujo marketplace.

## 0.1.1 typography and icon update

User requested Departure Mono wherever appropriate and Tabler icons to match kujolang.ai. Implemented in fea0af6; native verification/screenshots in 3437e99. Departure now supplies the native technical/code font variable, headings, tabs, keyboard labels and sidebar controls; conversation paragraphs remain sans. Tabler 3.46.0 is pinned locally: 149 shared bb names map to 139 outline SVGs. GitHub/Discord and provider marks stay native. The native icon registry and code-theme hook select Tabler only in Kujo dark; the documented nested same-name fallback restores builtins elsewhere and unload restores registration ownership. No DOM icon substitutions, observer, CDN or runtime icon font. Eleven tests/build/typecheck and native palette/light/unload/reload checks pass; repaired-editor workspace typing/undo/terminal QA passes with no page errors. App bundle is about 31.4 KB including icon geometry. Source mappings/license and exact scope are in docs/typography-icons.md. No new unresolved finding was produced; SignalBox capture is not warranted for this completed change. Recall: bb-kujo Departure Mono Tabler icon adapter.

## 0.2.0 light appearance — current state

User requested light mode using the existing SiteKit system. Source implementation e837aaf adds `palette-light.json` and `kujo-code-light.json` under the existing Kujo theme identity, with native codeTheme.dark/light selection. Light is SiteKit paper #f9f9f9, ink #060606, white raised surfaces and base semantic colors. Shared typography, Tabler registry and geometry apply to both modes; only other palettes restore native icons. Artwork remains original ink-on-paper at 6% opacity in light and inverted in dark. The same reduced-motion, hidden-tab pause and cleanup contract is retained. No new runtime loops/listeners or asset duplication. This supersedes revision 4's light-fallback behavior as of 2026-09-12.

Verification: 12 contracts/typecheck/native compiler pass; all light text/syntax/semantic roles exceed AA on neutral surfaces and conservative #d8d8d8 decorative composite. Native both-mode preview/screenshots, five reloads, disable/enable and default-theme restoration pass with no page errors. Native light Monaco color, keyboard typing/undo and terminal ANSI input/output pass with the already documented isolated editor repair; no stock upstream fix is implied. Screenshot harness uses keyboard events for editor input and isolated preview reloads between popup captures. Evidence: docs/light-release-*.json, docs/qa.md, screenshots/light-*.png. CSS is 143,194 characters, +8,274 over 0.1.1; short idle sample has no extra layouts or listener growth, not all-day certification.

Recall cues: bb-kujo light mode SiteKit paper ink palette; Kujo light Departure Mono Tabler; native light dark code theme. No new unresolved finding warrants SignalBox capture.

## Live chat walkthrough — 2026-09-12

User requested actual app opening/new chat/model response footage. Commit 4be9f4d adds the real 67.76-second 1600×1000/25fps MP4, capture script, receipt and response still under videos/. Delivered flow uses Codex 5.5 Medium on bb 0.43/Kujo 0.2.0, isolated thread thr_4vkayptes4. No synthetic response, retiming, voiceover or reconstructed UI. The model completes a read-only theme description; recording page errors are empty and the H.264 file decodes fully. Source WebM and unsuccessful preliminary takes are ignored artifacts. Read videos/README.md before repeating the capture.

Provider compatibility remains distinct from bb-kujo: the global custom catalog lacks supports_reasoning_summaries and supports_parallel_tool_calls required by CLI 0.144.4. Temporary copy plus local isolated-host executable wrapper unblocked parsing without global changes. The daemon's normal provider environment does not forward BB_CODEX_BRIDGE launch variables; its prepended executable directory accepts the temporary wrapper. 5.6-Sol subsequently rejected reasoning.context for Responses Lite, so the successful capture uses 5.5. The wrapper is a recording-only workaround, not plugin code. SignalBox capture cap_46365212-67ee-4f85-a81b-59ac8bd67f0c and signal sig_d98397c7-f03b-42fd-88f4-3d62bb2ed2d4 preserve the unresolved global catalog/CLI compatibility review; exact and concept retrieval passed. Completed video work itself was not captured there.

Recall cues: bb-kujo real video walkthrough Codex 5.5 recording; live chat theme description MP4; catalog CLI compatibility.
